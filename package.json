{
  "name": "@antid2ta/pi-extensions",
  "version": "0.1.0",
  "description": "Personal Pi extensions distributed as a Git-based npm workspaces monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/pi-agent-status",
    "packages/pi-mcp-adapter",
    "packages/pi-powerline-footer",
    "packages/pi-visual-profile"
  ],
  "pi": {
    "extensions": [
      "packages/*/index.ts"
    ],
    "themes": [
      "packages/*/themes/*.json"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit && npm run typecheck --workspaces",
    "test": "node --experimental-strip-types --test test/**/*.test.ts && npm run test --workspaces"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.84.4",
    "@earendil-works/pi-coding-agent": "0.84.4",
    "@earendil-works/pi-tui": "0.84.4",
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  }
}
